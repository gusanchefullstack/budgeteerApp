# Security

- In the first version of Budgeteer App, only username and password will be used for authentication.
- JWT Token must be provided once the user is registered or loggedIn. 
- JWT Token must me sent in header as a Bearer Token to authorizate the use of transaction and budget routes.
- Password must be hashed to be stored in database

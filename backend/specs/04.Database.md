# Database
- MongoDb Atlas is the service where the database will be hosted.
- Prisma.io ORM will be used for connecting to mongoDb Database
- DATABASE_URL is contained in .env file
- All access to DB via prisma orm must be implemented in the service layer. Proper error handling and exceptions must me implemented in service and controllers.
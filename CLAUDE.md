---
description: 
alwaysApply: true
---

# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Structure

This is a monorepo with two top-level directories:

- `backend/` — server-side code
- `frontend/` — client-side code

The VS Code workspace (`budgeteer.code-workspace`) opens both folders as separate workspace roots.

## Setup & Commands

_This project is in initial setup. Update this section once the stack is chosen and scaffolded._
- Always use npm instead of pnpm
